{% extends 'base.html.twig' %}
{% block title %}{% endblock %}

{% block body %}

    <div class="media">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }}</h1>
           <p>{{ program.summary }}</p>

            <div>
                Saisons:{{ season.description }}
            </div>
        </div>
    </div>

    <div>
        {% for episode in episodes %}
            <div>{{ episode.title }}</div>
        {% endfor %}
    </div>



    <a href="{{ path('wild_index') }}">
        Retour à l'accueil
    </a>

{% endblock %}
