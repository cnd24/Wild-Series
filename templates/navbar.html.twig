<nav class="navbar navbar-dark">
    <nav class="navbar navbar-expand-lg navbar-light">
        <a class="navbar-brand navbarTitle" href="{{ path('app_index') }}">Wild Series</a>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-item nav-link" href="{{ path('app_index') }}">Home</a>
                {% if app.user %}
                    <a class="nav-item nav-link" href="{{ path('program_index') }}">Séries-admin</a>
                    <a class="nav-item nav-link" href="{{ path('season_index') }}">Saisons-admin</a>
                    <a class="nav-item nav-link" href="{{ path('episode_index') }}">Episodes-admin</a>
                    <a class="nav-item nav-link" href="{{ path('category_add') }}">Catégories-admin</a>
                {% else %}
                    <a class="nav-item nav-link" href="{{ path('program_index') }}">Séries</a>
                {% endif %}
            </div>

        </div>
        <div class="text-align-right">
            <div class="navbar-nav">
            {% if app.user %}


                <a href="{{ path('app_logout') }}" class="nav-item nav-link">
                    <h6>{{ app.user.email }} - Logout <i class="fas fa-user-alt-slash ml-3"></i></h6>
                </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="nav-item nav-link">
                        <h6> Login <i class="fas fa-user ml-2"></i></h6>
                    </a>
                {% endif %}
            </div>
        </div>
    </nav>
</nav>
